# Optimal

Play with numbers.

## Installation

Add this line to your application's Gemfile:

    gem 'optimal'

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install optimal
